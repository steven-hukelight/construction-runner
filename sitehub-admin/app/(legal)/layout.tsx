/**
 * Legal route group layout.
 * Route group (legal) does not affect the URL path.
 */
export default function LegalLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
