-- Placeholder: this version exists in remote `supabase_migrations.schema_migrations` but the
-- original file was not in this repo. No-op so local migration filenames match remote history.
-- Safe: already applied on the linked project; will not re-run if history is consistent.

SELECT 1;
