-- No-op placeholder: version 20260401120000 is already recorded on the linked remote database
-- but had no matching file in this repo (branch / machine drift). This file satisfies the CLI
-- so `supabase db push` can proceed. It does not change schema (remote already ran the original migration).

SELECT 1;
